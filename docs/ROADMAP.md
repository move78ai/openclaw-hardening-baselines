# Roadmap (baseline repo)

v0.1.0
- Docker compose baseline
- Reverse proxy example
- Egress + skill governance templates
- act-check JSON score

Planned
- k8s baseline (minimal)
- more drift checks (egress exceptions, skill changes, file permission checks)
- example SIEM detections (vendor-neutral queries)
